# Source de donnees provisionnee au demarrage : aucune configuration
# manuelle n est necessaire, le dashboard est reproductible.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false